.modal.fade
  .modal-dialog{ style: 'min-width: 1024px;' }
    .modal-content
      .modal-header
        %button.close{ type: "button", data: { dismiss: "modal" } }
          &times;
            
        %h4.modal-title
          Export Report

      .modal-body
        %h4 Enter date range for report:
        = form_for report_exports_path() do |f|
          .form-content{ style: 'padding:10px;' }
            .form-group
              .row
                .col-md-2.col-sm-12
                  .form-group
                    = f.label :starts_at, class: 'control-label'
                    = f.text_field :starts_at, value: "#{Time.now.strftime('%Y')}/01/01", class: 'form-control date-editor', placeholder: 'YYYY/MM/DD'
                .col-md-2.col-sm-12
                  .form-group
                    = f.label :ends_at, class: 'control-label'
                    = f.text_field :ends_at, value: DateTime.now.to_s, class: 'form-control date-editor', placeholder: 'YYYY/MM/DD'
          = f.submit 'View exports', class: 'btn btn-primary'
